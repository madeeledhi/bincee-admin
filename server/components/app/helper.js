// import { handleFetchResponse, handleError } from '../../utils/errorUtils'

/**
 * Helper for Aera Recommends request
 * @param {$Request} reqContext
 //  */
// export default reqContext => {
//   const { res, token, dataSource } = reqContext

//   return fetch(`${dataSource}?token=${token}`)
//     .then(rsp => handleFetchResponse(rsp))
//     .catch(error => handleError(error, res))
// }
